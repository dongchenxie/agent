{
  "name": "email-loop-agent",
  "version": "1.0.3",
  "description": "Distributed email sending agent for Email Loop",
  "main": "index.ts",
  "scripts": {
    "start": "bun run index.ts",
    "dev": "bun run --watch index.ts"
  },
  "dependencies": {
    "nodemailer": "^7.0.12"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "@types/nodemailer": "^7.0.5"
  }
}
